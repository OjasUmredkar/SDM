create table customer(
custid int,
cname varchar(20),
address varchar(20),
adharno long
);

create table destination(
trainno int,
tname varchar(20),
tdest varchar(20)
);

create table flight(
flightno int,
fname varchar(20),
fdest varchar(20)
);
create table destination(
Did int,
dname varchar(20)
);

